<Page
    x:Class="UWPBank.RootVisual"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWPBank"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel>
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Margin" Value="0, 2" />
                </Style>
            </StackPanel.Resources>
            <Button Content="Launch Desktop Process" Command="{Binding NavigationCommand}" CommandParameter="UWPBank.Desktop.LaunchDesktopProcessPage" />
            <Button Content="Trigger an app crash for HockeyApp" Command="{Binding TriggerHockeyAppCrashCommand}" />
            <Button Content="Show me some advertisements" Command="{Binding NavigationCommand}" CommandParameter="UWPBank.AdvertisementPage" />
            <Button Content="Consume appservice provided by another UWP app" Command="{Binding NavigationCommand}" CommandParameter="UWPBank.AppServiceConsumerPage" />
            <Button Content="Show a Map" Command="{Binding NavigationCommand}" CommandParameter="UWPBank.MapControlPage" />
        </StackPanel>
    </Grid>
</Page>
