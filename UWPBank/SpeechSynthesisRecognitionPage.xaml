<Page
    x:Class="UWPBank.SpeechSynthesisRecognitionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWPBank"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" DataContext="{Binding UWPBankViewModelInstance, Source={StaticResource Locator}}">
    <Page.BottomAppBar>
        <CommandBar ClosedDisplayMode="Minimal">
            <AppBarButton Icon="Home" Label="Home" Command="{Binding NavigationToHomeCommand}" />
        </CommandBar>
    </Page.BottomAppBar>
    
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Margin="30">
            <TextBlock Text="Speech Synthesis and Recognition" Style="{StaticResource PageTitleStyle}" />
            <TextBlock Text="Select Voice" />
            <ComboBox x:Name="cbAvailableVoices" Width="200" />
            <TextBlock Text="Select a text file and read its content" />
            <Button Content="Select File" Click="btnSelectTextToRead_Click" />
            <Grid Height="40" />
            <TextBlock Text="Machine asks: What is your phone number?" />
            <Button Content="And you say:" Click="btnSpeechRecognize_Click" />
            <TextBlock Foreground="Green" x:Name="tbPhoneNumber" />
        </StackPanel>
    </Grid>
</Page>
